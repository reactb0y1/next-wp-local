import React from 'react';
import styled from '@emotion/styled';

export const Header = () => {
  return <StyledHeader className={'header'}>Header</StyledHeader>;
};

const StyledHeader = styled.header``;
