import React from 'react';
import styled from '@emotion/styled';

export const MainHome = () => {
  return <StyledMainHome className={'main'}>Main Home</StyledMainHome>;
};

const StyledMainHome = styled.main``;
